{
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "concurrently -k -n worker,frontend -c cyan,magenta \"pnpm --filter ./worker run dev\" \"pnpm --filter ./frontend run dev\"",
    "build": "pnpm --filter ./worker run build && pnpm --filter ./frontend run build",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vitest": "^4.1.2"
  }
}
